[project]
name = "bureau"
version = "0.1.0"
description = "Bureau agent orchestration framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk",
    "aiohttp",
    "ollama",
    "ollama-mcp-bridge>=0.11.2",
]

[dependency-groups]
dev = []
