[project]
name = "bureau"
version = "0.1.0"
description = "Bureau agent orchestration framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk",
    "aiohttp",
    "ollama",
]

[tool.uv]
dev-dependencies = []
